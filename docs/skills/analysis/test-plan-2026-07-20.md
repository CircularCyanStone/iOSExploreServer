# ios-automation + ios-connection 测试计划

**日期**: 2026-07-20  
**测试范围**: Phase 1（MCP 违规修复）+ Phase 2（skill 拆分）  
**测试目标**: 确保修改后的 skills 功能完整、规范合规、实际可用  

---

## 测试分层

### L1: 静态验证（规范合规性）
- frontmatter 完整性
- description 中英混合
- allowed-tools 与实际调用一致
- 无 SPMExample 硬编码
- 无 Agent 调用 bash 指令

### L2: 结构验证（内容完整性）
- ios-automation 保留了必要内容（MCP 检测、路由表）
- ios-connection 包含了所有连接管理内容
- 两个 skill 相互引用正确
- inventory.md 更新正确

### L3: 功能验证（模拟器）
- 模拟器环境下 health_check 成功路径
- 路由到各个子 skill 正确
- MCP 不可用时给出配置提示

### L4: 功能验证（真机，可选）
- 真机连接流程（如果有真机）
- iproxy 管理与诊断
- 端口冲突场景处理

---

## 测试用例清单

### 静态验证（15 项）

#### ios-automation
1. frontmatter 包含 name/description/allowed-tools ✓
2. description 中英混合 ✓
3. allowed-tools 只包含 5 个工具（health_check/ui_inspect/ui_tap_and_inspect/app_logs_read/list_devices）✓
4. 正文无 SPMExample 硬编码（UDID、bundle ID、具体路径）✓
5. 正文无 Agent 调用 bash 指令（grep "Agent.*bash|Agent.*curl|Agent.*scripts/proxy.sh"）✓
6. 保留了 MCP 依赖检测小节 ✓
7. 保留了 L0 vs L1 选择规则小节 ✓
8. 保留了路由到子 skill 小节 ✓
9. 提及 ios-connection 作为连接问题的路由目标 ✓

#### ios-connection
10. frontmatter 包含 name/description/allowed-tools ✓
11. description 中英混合 ✓
12. allowed-tools 包含 9 个工具（完整设备管理与 App 启动工具）✓
13. 正文无 SPMExample 硬编码 ✓
14. 包含"连接管理"小节（模拟器/真机/四个差异）✓
15. 包含"快速诊断"和"常见错误与判别"小节 ✓

#### inventory.md
16. ios-connection 条目存在且完整 ✓
17. ios-automation 条目已更新（备注提及拆分）✓
18. 计数核对正确（13 → 14）✓

### 结构验证（8 项）

19. ios-automation 不包含 iproxy 详细管理流程（grep -c "iproxy" 应 <= 5）✓
20. ios-connection 包含 iproxy 详细说明（grep -c "iproxy" 应 >= 15）✓
21. ios-automation 相互引用：提及 ios-connection 至少 5 次 ✓
22. ios-connection 相互引用：提及 ios-automation 至少 3 次 ✓
23. ios-automation 路由表包含至少 10 个路由规则 ✓
24. ios-connection 包含 5 种常见错误的判别 ✓
25. 两个 skill 的 allowed-tools 没有重复过多（允许少量重叠如 health_check）✓
26. ios-automation 行数在 150-250 范围 ✓
27. ios-connection 行数在 150-250 范围 ✓

### 功能验证 - 模拟器（6 项）

28. 模拟器环境：调用 ios-automation，MCP 可用时继续执行
29. 模拟器环境：health_check 成功时，能正确路由到 ios-ui-form
30. 模拟器环境：health_check 成功时，能正确路由到 ios-ui-alert
31. 模拟器环境：health_check 失败时，提示路由到 ios-connection
32. 模拟器环境：iOSDriver MCP 不可用时，给出配置提示并停止
33. 模拟器环境：调用 ios-connection，能读取当前连接状态

### 功能验证 - 真机（4 项，可选）

34. 真机环境：调用 ios-connection，能检测 iproxy 状态
35. 真机环境：list_devices 返回真机信息
36. 真机环境：health_check 能连接到真机 App
37. 真机环境：端口冲突诊断流程能给出明确提示

---

## 测试执行策略

### Phase A: 静态验证（Subagent 1，立即执行）
- 执行测试用例 1-27
- 使用 grep/wc/Read 验证
- 输出：静态验证报告（通过/失败清单）

### Phase B: 功能验证 - 模拟器（Subagent 2，立即执行）
- 执行测试用例 28-33
- 前置条件：SPMExample 在模拟器运行
- 使用 XcodeBuildMCP + iOSDriver MCP 实际调用
- 输出：功能验证报告（每个场景的实际行为）

### Phase C: 功能验证 - 真机（手动，可选）
- 执行测试用例 34-37
- 需要真机连接
- 输出：真机测试报告

---

## 成功标准

- **静态验证**：27 项全部通过
- **功能验证 - 模拟器**：6 项至少 5 项通过（允许 1 项因环境问题跳过）
- **功能验证 - 真机**：可选，如果执行则至少 3 项通过

---

## 失败处理

- 任何静态验证失败 → 立即修复 skill 文件
- 功能验证失败 → 分析原因（skill 问题 vs 环境问题）
- 记录所有失败项到测试报告
