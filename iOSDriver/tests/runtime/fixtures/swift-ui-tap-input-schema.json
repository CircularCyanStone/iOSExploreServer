{
  "type": "object",
  "properties": {
    "accessibilityIdentifier": {
      "type": [
        "string",
        "null"
      ],
      "description": "按 accessibilityIdentifier 定位。"
    },
    "path": {
      "type": [
        "string",
        "null"
      ],
      "description": "按 path 定位。"
    },
    "viewSnapshotID": {
      "type": "string",
      "description": "ui.inspect 签发的快照标识。"
    }
  },
  "required": [
    "viewSnapshotID"
  ],
  "additionalProperties": false,
  "x-iosExplore-propertyOrder": [
    "accessibilityIdentifier",
    "path",
    "viewSnapshotID"
  ],
  "oneOf": [
    {
      "required": [
        "accessibilityIdentifier"
      ]
    },
    {
      "required": [
        "path"
      ]
    }
  ],
  "x-iosExplore-constraints": [
    "定位字段按非 null、非空值计算，path 文法由 UIKit locator parser 校验；命令不接受 x/y/coordinateSpace，且 viewSnapshotID 必填。"
  ]
}
