{
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "mode": {
      "type": "string",
      "enum": ["idle", "targetExists", "targetGone", "textExists", "snapshotChanged"]
    }
  },
  "required": ["id", "mode"],
  "additionalProperties": true
}
